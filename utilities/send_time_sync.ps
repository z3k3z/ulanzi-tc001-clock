// copy-paste this into a powershell window
$p = New-Object System.IO.Ports.SerialPort COM3,115200; $p.Open(); Start-Sleep -Seconds 2; $p.WriteLine("TIME:$([int][double]::Parse((Get-Date -UFormat %s)))"); $p.Close()
